{
  "name": "space-combat-mp",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@colyseus/schema": "2.0.37"
  },
  "pnpm": {
    "overrides": {
      "@colyseus/schema": "2.0.37"
    },
    "publicHoistPattern": [
      "@colyseus/schema"
    ]
  },
  "scripts": {
    "dev:server": "pnpm --filter @space-combat/server dev",
    "dev:client": "pnpm --filter @space-combat/client dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
